---
title: 14 - Longest Common Prefix
date: 2021-06-07
category: javascript
published: true
coverImg: https://images.unsplash.com/photo-1597423244036-ef5020e83f3c?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1567&q=80
photographer: Dan-Cristian Paduret
keywords: ["Leetcode", "Algorithms"]
---

```js
const longestCommonPrefix = function (strs) {
  let result = ""
  // if there is an empty array
  if (!strs.length) {
    return result
  }

  if (
    !strs.every(el => {
      return el !== ""
    })
  ) {
    return result
  }

  if (strs.length === 1) {
    return strs.join("")
  }

  // Check if there are any common prefixes.
  // Checking the first index
  for (let i = 0; i < strs[0].length; i++) {
    if (strs.every(el => el[i] === strs[0][i])) {
      result += strs[0][i]
    } else {
      break
    }
  }

  return result
}
```

Time Complexity O(n^3) Not good:(
Space Complexity O(n)
