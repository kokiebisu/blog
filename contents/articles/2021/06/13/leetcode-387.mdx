---
title: 387
date: 2021-06-13
category: leetcode
published: true
coverImg: https://kocoblo-static.s3.amazonaws.com/2021/06/13/leetcode-268/cover-img.jpg
photographer: Sam Fry
keywords: ["Module System"]
---

Brute Force

```js
- @param {string} s
- @return {number}
  var firstUniqChar = function (s) {
  let stack = []
  const map = {}
  for (let i = 0; i < s.length; i++) {
  if (!map[s[i]]) {
  // add it to stack
  stack.push({ index: i, char: s[i] })
  // add it to map
  map[s[i]] = true
  } else {
  stack = stack.filter(el => el.char !== s[i])
  }
  }
  if (!stack.length) {
  return -1
  }
  return stack[0]["index"]
  }


```
