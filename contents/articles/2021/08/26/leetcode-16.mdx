---
title: 16. 3Sum Closest
date: 2021-08-26
category: algorithms
published: true
coverImg: https://images.unsplash.com/photo-1629961472029-da08ef36112f?ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&ixlib=rb-1.2.1&auto=format&fit=crop&w=1050&q=80
photographer: Tom Tor
keywords: []
---

```js
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var threeSumClosest = function (nums, target) {
  nums = nums.sort((a, b) => a - b)
  let result = 0
  let diff = Infinity
  for (let i = 0; i < nums.length - 2; i++) {
    let low = i + 1
    let high = nums.length - 1
    let total = nums[i] + nums[low] + nums[high]
    while (low < high) {
      total = nums[i] + nums[low] + nums[high]
      if (Math.abs(total - target) < diff) {
        diff = Math.abs(total - target)
        result = total
      }
      if (total < target) {
        low++
      } else if (total > target) {
        high--
      } else {
        return result
      }
    }
  }
  return result
}
```
