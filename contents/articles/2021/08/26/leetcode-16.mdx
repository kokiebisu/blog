---
title: すごい人脈術
date: 2021-08-23
category: network
published: true
coverImg: https://images.unsplash.com/photo-1519530720729-a2effdafab20?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1650&q=80
photographer: Nqobile Vundla
keywords: ["indexOf"]
reference: ２０代のうちに知っておくべき「お金の稼ぎ方」を教えよう
author: 金川顕教
---

```js

/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
 var threeSumClosest = function(nums, target) {
    nums = nums.sort((a, b) => a - b);
    let result = 0;
    let diff = Infinity;
    for (let i = 0; i < nums.length - 2; i++) {
        let low = i + 1;
        let high = nums.length - 1
        let total = nums[i] + nums[low] + nums[high]
        while (low < high) {
            total = nums[i] + nums[low] + nums[high]
            if (Math.abs(total - target) < diff) {
                diff = Math.abs(total - target)
                result = total
            }
            if (total < target) {
                low++;
            } else if (total > target) {
                high--;
            } else {
                return result
            }
        }
    }
    return result
};
```